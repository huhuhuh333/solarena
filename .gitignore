# secrets — NEVER commit
.env*

# data & artifacts
server/data/
dist/
node_modules/
scripts/shots/
scripts/.smoke.cjs
# compiled test doubles — regenerated by `npm run escrow:build`
scripts/escrow-mocks.json
# throwaway database the escrow rail test builds from scratch each run
server/data/escrow-rail-test.db*

# logs and local tool state
*.log
.claude/
